import SwiftUI

enum AtayaTheme {
    static let yellow = Color(red: 247/255, green: 212/255, blue: 76/255)
    static let card = Color(.systemBackground)
    static let border = Color(.systemGray4)

    static let bg = Color(.systemGroupedBackground)
}
